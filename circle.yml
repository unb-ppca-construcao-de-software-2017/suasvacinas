machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull node:8.1.2
    - docker build -t suasvacinas-ci .

test:
  override:
    # figure out how to run tests before
    # npm run test
    # npm run e2e
    - docker run -it --env FIREBASE_TOKEN=$FIREBASE_TOKEN suasvacinas-ci npm run deploy-firebase
